name: Test run (single video)

on:
  workflow_dispatch:
    inputs:
      topic:
        description: 'Optional topic override (leave blank to research trending)'
        required: false
        default: ''

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'   # chatterbox-tts requires 3.11 specifically

      - name: Cache voice + caption models
        uses: actions/cache@v4
        with:
          path: hf_cache/
          # Holds both Chatterbox-Turbo (voice.py) and faster-whisper
          # (assemble.py) downloads -- HF_HOME points both at this same
          # dir. Bump this suffix again any time either model changes,
          # to force a fresh combined cache instead of reusing a stale one.
          key: chatterbox-whisper-models-v1

      - name: Install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Run channels
        run: python main.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          # Existing GitHub secrets mapped to NOVA_* env names used by engine/config.py
          NOVA_GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          NOVA_GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          NOVA_GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}
          NOVA_DRIVE_FOLDER_ID: ${{ secrets.DRIVE_FOLDER_ID }}
          NOVA_DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          HETTI_DISCORD_WEBHOOK_URL: ${{ secrets.HETTI_DISCORD_WEBHOOK_URL }}
          HETTI_GOOGLE_CLIENT_ID: ${{ secrets.HETTI_GOOGLE_CLIENT_ID }}
          HETTI_GOOGLE_CLIENT_SECRET: ${{ secrets.HETTI_GOOGLE_CLIENT_SECRET }}
          HETTI_GOOGLE_REFRESH_TOKEN: ${{ secrets.HETTI_GOOGLE_REFRESH_TOKEN }}
          HETTI_DRIVE_INTAKE_FOLDER_ID: ${{ secrets.HETTI_DRIVE_INTAKE_FOLDER_ID }}
          HETTI_DRIVE_OUTPUT_FOLDER_ID: ${{ secrets.HETTI_DRIVE_OUTPUT_FOLDER_ID }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          REDDIT_CLIENT_ID: ${{ secrets.REDDIT_CLIENT_ID }}
          REDDIT_CLIENT_SECRET: ${{ secrets.REDDIT_CLIENT_SECRET }}
          # Blank input => trending research in main.py
          VIDEO_TOPIC: ${{ inputs.topic }}
          HF_HOME: ${{ github.workspace }}/hf_cache